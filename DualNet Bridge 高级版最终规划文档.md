> # DualNet Bridge 高级版最终规划文档
>
> ## 1. 项目定位
>
> **DualNet Bridge** 是一款双端 Windows 网络共享工具，用于在不修改第一台电脑公司网络配置的前提下，让第二台电脑通过第一台电脑访问公网。
>
> 一句话定义：
>
> > 在第一台电脑公司网卡保持 DHCP 自动获取的前提下，将第一台电脑变成受控网关，使第二台电脑自动通过它上网。
>
> ------
>
> ## 2. 核心使用场景
>
> 用户有两台电脑，但公司网络只给一台电脑分配可用网络出口。
>
> 现状问题：
>
> ```text
> 1. 第一台电脑可以上网
> 2. 第二台电脑无法上网
> 3. 两台电脑手动设置 IP 后可以互相 ping
> 4. 但第二台电脑无法访问公网
> 5. 第一台电脑的公司网络配置不能乱改，必须保持自动获取
> ```
>
> 本软件要解决的问题：
>
> ```text
> 1. 第一台电脑继续正常使用公司网络
> 2. 第二台电脑通过第一台电脑访问公网
> 3. 两台电脑之间自动建立内网连接
> 4. 软件自动配置、诊断、恢复
> 5. 不需要用户手动理解 NAT、网关、DNS、路由表这些人类文明的边角料
> ```
>
> ------
>
> ## 3. 最高优先级原则
>
> ### 3.1 第一台电脑公司网卡只读保护
>
> 第一台电脑连接公司网络的网卡必须保持：
>
> ```text
> 自动获取 IP
> 自动获取网关
> 自动获取 DNS
> 保持公司 DHCP / 准入 / 域控配置
> ```
>
> 软件绝对不能修改公司网卡的：
>
> ```text
> 1. IP 地址
> 2. 子网掩码
> 3. 默认网关
> 4. DNS
> 5. MAC 地址
> 6. DHCP 模式
> 7. 802.1X / NAC / 公司准入配置
> 8. 域控相关配置
> ```
>
> 软件只能读取公司网卡状态，例如：
>
> ```text
> 1. 当前 IP
> 2. 当前网关
> 3. 当前 DNS
> 4. 是否联网
> 5. 是否为默认出口
> 6. 网卡接口索引
> 7. 当前路由表
> ```
>
> ### 3.2 只能修改内网侧
>
> 软件可以修改的对象只有：
>
> ```text
> 1. 第一台电脑连接第二台电脑的内网网卡
> 2. 第二台电脑连接第一台电脑的网卡
> 3. 本软件创建的 NAT 规则
> 4. 本软件创建的 DNS / DHCP / 代理服务
> 5. 本软件创建的防火墙规则
> ```
>
> ------
>
> ## 4. 推荐硬件连接方式
>
> ### 4.1 推荐拓扑
>
> ```text
> 公司网络
>   ↓
> 第一台电脑 网卡 A：公司网卡，DHCP 自动获取，只读保护
>   ↓
> 第一台电脑 网卡 B：内网网卡，由软件配置
>   ↓
> 第二台电脑 网卡：由客户端自动配置
> ```
>
> ### 4.2 推荐设备
>
> 第一台电脑最好有两个网络接口：
>
> ```text
> 1. 原有网口 / Wi-Fi：连接公司网络
> 2. USB 千兆网卡 / 第二网口：连接第二台电脑
> ```
>
> 推荐结构：
>
> ```text
> 公司网线 / 公司 Wi-Fi
>   ↓
> 第一台电脑
>   ↓
> USB 千兆网卡 / 第二网口
>   ↓
> 第二台电脑
> ```
>
> 也可以使用：
>
> ```text
> 第一台电脑开启 Wi-Fi 热点
> 第二台电脑连接该热点
> ```
>
> 但更推荐有线直连或 USB 网卡，因为稳定、延迟低、可控性更好。
>
> ------
>
> ## 5. 双端软件架构
>
> 高级体验采用双端结构。
>
> ```text
> 第一台电脑：Gateway 主控端
> 第二台电脑：Client 客户端
> ```
>
> ### 5.1 Gateway 主控端职责
>
> 安装在第一台电脑。
>
> 负责：
>
> ```text
> 1. 自动识别公司出口网卡
> 2. 将公司出口网卡标记为受保护网卡
> 3. 禁止修改公司出口网卡配置
> 4. 自动识别内网网卡
> 5. 配置内网网卡 IP
> 6. 创建 NAT 转发
> 7. 启动 DNS 转发服务
> 8. 可选启动 DHCP 服务
> 9. 可选启动 HTTP / SOCKS5 代理
> 10. 接收客户端连接
> 11. 展示第二台电脑联网状态
> 12. 执行网络诊断
> 13. 一键关闭共享
> 14. 一键恢复原配置
> ```
>
> ### 5.2 Client 客户端职责
>
> 安装在第二台电脑。
>
> 负责：
>
> ```text
> 1. 自动发现第一台电脑 Gateway
> 2. 自动识别当前连接第一台电脑的网卡
> 3. 备份第二台电脑原网络配置
> 4. 自动配置 IP / 网关 / DNS
> 5. 自动测试与 Gateway 的连通性
> 6. 自动测试公网 IP 连通性
> 7. 自动测试 DNS 解析
> 8. 将诊断结果回传给 Gateway
> 9. 一键恢复第二台电脑原网络配置
> ```
>
> ------
>
> ## 6. 网络模式设计
>
> 软件提供三种模式。
>
> ------
>
> ### 6.1 高级 NAT 模式，默认推荐
>
> 这是主模式。
>
> 结构：
>
> ```text
> 第二台电脑
>   ↓
> 第一台电脑内网网卡
>   ↓
> Windows NAT
>   ↓
> 第一台电脑公司网卡
>   ↓
> 公网
> ```
>
> 默认内网网段：
>
> ```text
> 192.168.77.0/24
> ```
>
> 第一台电脑内网网卡：
>
> ```text
> IP：192.168.77.1
> 子网掩码：255.255.255.0
> 默认网关：不设置
> DNS：可不设置，或指向自身
> ```
>
> 第二台电脑：
>
> ```text
> IP：192.168.77.100
> 子网掩码：255.255.255.0
> 默认网关：192.168.77.1
> DNS：192.168.77.1
> ```
>
> 关键点：
>
> ```text
> 第一台电脑公司网卡继续 DHCP 自动获取。
> 软件不修改公司网卡。
> NAT 只针对 192.168.77.0/24 内网段。
> ```
>
> 优点：
>
> ```text
> 1. 第二台电脑接近全局联网
> 2. 不依赖单个软件是否支持代理
> 3. 第一台公司网卡不被修改
> 4. 可控性高
> 5. 适合高级体验
> ```
>
> 缺点：
>
> ```text
> 1. 双端都需要管理员权限
> 2. 需要处理 NAT、DNS、防火墙
> 3. 最好有独立内网网卡
> ```
>
> ------
>
> ### 6.2 ICS 兼容模式，备用
>
> 使用 Windows Internet Connection Sharing。
>
> 结构：
>
> ```text
> 第二台电脑
>   ↓
> 第一台电脑 ICS
>   ↓
> 第一台电脑公司网卡
>   ↓
> 公网
> ```
>
> 特点：
>
> ```text
> 1. Windows 原生功能
> 2. 实现简单
> 3. 但可控性较差
> 4. 默认可能使用 192.168.137.0/24
> 5. 部分公司策略可能禁用
> ```
>
> ICS 模式仍然必须遵守：
>
> ```text
> 公司网卡只作为共享来源
> 不修改公司网卡 DHCP / DNS / 网关
> 只修改内网目标网卡
> ```
>
> ------
>
> ### 6.3 代理模式，兜底
>
> Gateway 启动：
>
> ```text
> HTTP 代理
> SOCKS5 代理
> ```
>
> Client 自动设置系统代理。
>
> 默认：
>
> ```text
> HTTP：192.168.77.1:7890
> SOCKS5：192.168.77.1:1080
> ```
>
> 适合：
>
> ```text
> 1. 浏览器
> 2. Git
> 3. npm
> 4. pip
> 5. 部分开发工具
> ```
>
> 限制：
>
> ```text
> 1. 不是完整全局网络
> 2. ping 不走代理
> 3. UDP 支持有限
> 4. 游戏和部分系统服务可能无效
> ```
>
> 代理模式不作为主方案，只作为低风险兜底。
>
> ------
>
> ## 7. 受保护网卡机制
>
> 软件必须实现一个核心对象：
>
> ```text
> ProtectedAdapter
> ```
>
> ### 7.1 识别规则
>
> 满足以下条件的网卡应被标记为受保护网卡：
>
> ```text
> 1. 当前系统默认路由所在网卡
> 2. 拥有默认网关
> 3. DHCP 已启用
> 4. 能访问公网
> 5. 连接公司网络
> ```
>
> 判断默认出口：
>
> ```text
> 查找 0.0.0.0/0 默认路由指向的 Interface。
> 该 Interface 对应的网卡就是当前出口网卡。
> ```
>
> ### 7.2 禁止操作
>
> 对 ProtectedAdapter 禁止：
>
> ```text
> 1. 设置静态 IP
> 2. 修改 DNS
> 3. 修改默认网关
> 4. 禁用网卡
> 5. 重启网卡
> 6. 创建网桥
> 7. 修改 MAC
> 8. 清除 DHCP 配置
> ```
>
> ### 7.3 允许操作
>
> 只允许：
>
> ```text
> 1. 读取配置
> 2. 读取 DNS
> 3. 读取网关
> 4. 检测连通性
> 5. 读取路由表
> 6. 读取接口索引
> ```
>
> 如果用户选择公司网卡作为内网网卡，软件必须阻止：
>
> ```text
> 该网卡是当前公司网络出口，已被保护。
> 请选择连接第二台电脑的独立内网网卡。
> ```
>
> ------
>
> ## 8. 核心模块设计
>
> ### 8.1 网卡扫描模块
>
> 负责：
>
> ```text
> 1. 获取所有网卡
> 2. 判断网卡类型
> 3. 判断是否启用
> 4. 判断是否有 IP
> 5. 判断是否有默认网关
> 6. 判断是否 DHCP
> 7. 判断是否可访问公网
> 8. 区分物理网卡、USB 网卡、虚拟网卡、VPN 网卡
> ```
>
> 输出：
>
> ```text
> 1. 公司出口网卡
> 2. 受保护网卡
> 3. 可用于内网连接的候选网卡
> 4. 风险网卡
> 5. 虚拟网卡
> ```
>
> ------
>
> ### 8.2 NAT 管理模块
>
> 负责：
>
> ```text
> 1. 创建 NAT 规则
> 2. 删除 NAT 规则
> 3. 查询 NAT 状态
> 4. 检查 NAT 是否冲突
> 5. 检查网段是否冲突
> 6. 回滚 NAT 配置
> ```
>
> 默认 NAT 名称：
>
> ```text
> DualNetBridgeNat
> ```
>
> 默认 NAT 网段：
>
> ```text
> 192.168.77.0/24
> ```
>
> 注意：
>
> ```text
> NAT 模块不指定、不修改公司网卡。
> 出站路径由 Windows 当前默认路由决定。
> ```
>
> ------
>
> ### 8.3 内网网卡配置模块
>
> 负责配置第一台电脑的内网网卡。
>
> 默认配置：
>
> ```text
> IP：192.168.77.1
> 子网掩码：255.255.255.0
> 默认网关：空
> DNS：空或 127.0.0.1
> ```
>
> 禁止：
>
> ```text
> 给第一台电脑内网网卡设置默认网关
> ```
>
> 原因：
>
> ```text
> 第一台电脑只能有一个主要默认出口。
> 如果内网网卡设置默认网关，可能会破坏主电脑上网路由。
> ```
>
> ------
>
> ### 8.4 Client 自动配置模块
>
> 负责配置第二台电脑。
>
> 默认配置：
>
> ```text
> IP：192.168.77.100
> 子网掩码：255.255.255.0
> 默认网关：192.168.77.1
> DNS：192.168.77.1
> ```
>
> 配置前必须备份：
>
> ```text
> 1. 原 IP 模式
> 2. 原 IP 地址
> 3. 原 DNS
> 4. 原网关
> 5. 原代理设置
> ```
>
> 支持两种方式：
>
> ```text
> 1. 静态配置
> 2. DHCP 自动获取
> ```
>
> MVP 推荐先做静态配置，后续再增加 DHCP。
>
> ------
>
> ### 8.5 DNS 转发模块
>
> Gateway 启动本地 DNS 转发服务。
>
> Client 的 DNS 指向：
>
> ```text
> 192.168.77.1
> ```
>
> Gateway 上游 DNS 优先使用：
>
> ```text
> 第一台电脑公司网卡当前 DNS
> ```
>
> 备用 DNS：
>
> ```text
> 223.5.5.5
> 114.114.114.114
> ```
>
> DNS 策略：
>
> ```text
> 1. 优先走公司 DNS
> 2. 公司 DNS 失败后走备用 DNS
> 3. 支持 DNS 缓存
> 4. 支持内网域名优先公司 DNS
> 5. 不强制使用外部 DNS
> ```
>
> 原因：
>
> ```text
> 公司内网系统可能依赖公司 DNS。
> 如果强行使用公共 DNS，内部域名可能无法解析。
> ```
>
> ------
>
> ### 8.6 DHCP 模块，第二阶段
>
> Gateway 可选启动 DHCP 服务。
>
> 默认 DHCP 池：
>
> ```text
> 192.168.77.100 - 192.168.77.200
> ```
>
> 下发：
>
> ```text
> 网关：192.168.77.1
> DNS：192.168.77.1
> 子网掩码：255.255.255.0
> ```
>
> MVP 不强制做 DHCP。
>
> 高级版本再做。
>
> ------
>
> ### 8.7 代理模块
>
> Gateway 可选启动：
>
> ```text
> HTTP 代理
> HTTPS CONNECT 代理
> SOCKS5 代理
> ```
>
> 默认只监听内网网卡：
>
> ```text
> 192.168.77.1
> ```
>
> 不允许默认监听：
>
> ```text
> 0.0.0.0
> ```
>
> 默认端口：
>
> ```text
> HTTP：7890
> SOCKS5：1080
> ```
>
> 安全策略：
>
> ```text
> 1. 只允许内网访问
> 2. 可设置账号密码
> 3. 不暴露到公司外网网卡
> 4. 关闭共享时自动关闭代理
> ```
>
> ------
>
> ### 8.8 防火墙模块
>
> 负责创建和删除软件专属规则。
>
> 允许：
>
> ```text
> 1. Client 访问 Gateway DNS 端口
> 2. Client 访问 Gateway DHCP 端口
> 3. Client 访问 Gateway 代理端口
> 4. Client ping Gateway
> 5. Gateway 与 Client 通信
> ```
>
> 防火墙规则必须限定：
>
> ```text
> 只对内网网卡生效
> 只对 192.168.77.0/24 生效
> ```
>
> 禁止：
>
> ```text
> 把代理、DNS、DHCP 服务开放到公司网卡
> ```
>
> ------
>
> ### 8.9 双端发现模块
>
> Client 自动发现 Gateway。
>
> 推荐方式：
>
> ```text
> UDP 广播 + 手动输入 IP 兜底
> ```
>
> Gateway 监听：
>
> ```text
> 192.168.77.1:37777
> ```
>
> Client 广播：
>
> ```text
> DISCOVER_DUALNET_GATEWAY
> ```
>
> Gateway 返回：
>
> ```json
> {
>   "type": "DUALNET_GATEWAY",
>   "gateway_ip": "192.168.77.1",
>   "network": "192.168.77.0/24",
>   "dns": "192.168.77.1",
>   "mode": "nat",
>   "version": "1.0.0"
> }
> ```
>
> 如果自动发现失败，Client 允许用户手动输入：
>
> ```text
> 192.168.77.1
> ```
>
> ------
>
> ### 8.10 诊断模块
>
> Gateway 诊断：
>
> ```text
> 1. 公司网卡是否正常联网
> 2. 公司网卡是否保持 DHCP
> 3. 公司网卡是否被保护
> 4. 内网网卡是否配置成功
> 5. NAT 是否存在
> 6. DNS 转发是否正常
> 7. Client 是否在线
> 8. Client 是否能访问公网
> ```
>
> Client 诊断：
>
> ```text
> 1. 是否能 ping 192.168.77.1
> 2. 是否能访问公网 IP
> 3. 是否能解析域名
> 4. 默认网关是否为 192.168.77.1
> 5. DNS 是否为 192.168.77.1
> 6. 是否存在多个默认网关
> 7. 是否存在网段冲突
> ```
>
> 典型判断：
>
> ```text
> 能 ping 192.168.77.1，但不能 ping 公网 IP：
> NAT / 防火墙 / 路由问题
> 
> 能 ping 公网 IP，但不能访问域名：
> DNS 问题
> 
> 不能 ping 192.168.77.1：
> 双机内网连接问题
> 
> 第一台电脑不能上网：
> 公司网卡异常，但软件不得自动修改公司网卡
> ```
>
> ------
>
> ### 8.11 回滚模块
>
> 这是核心模块，必须优先开发。
>
> Gateway 回滚：
>
> ```text
> 1. 删除 NAT 规则
> 2. 停止 DNS 转发
> 3. 停止 DHCP
> 4. 停止代理
> 5. 删除防火墙规则
> 6. 恢复内网网卡原配置
> 7. 保持公司网卡不动
> ```
>
> Client 回滚：
>
> ```text
> 1. 恢复原 IP 模式
> 2. 恢复原 DNS
> 3. 恢复原网关
> 4. 恢复原代理设置
> 5. 删除客户端临时配置
> ```
>
> 异常恢复：
>
> ```text
> 1. 软件崩溃后，下次启动检测残留配置
> 2. 电脑重启后，检测 NAT / DNS / 防火墙是否残留
> 3. 提供一键恢复
> 4. 提供独立 reset 脚本
> ```
>
> ------
>
> ## 9. 软件技术栈
>
> ### 9.1 桌面端
>
> 推荐：
>
> ```text
> Tauri
> Vue 3
> TypeScript
> Pinia
> Vite
> Rust
> ```
>
> 原因：
>
> ```text
> 1. Tauri 体积小
> 2. Rust 适合系统级网络配置
> 3. Vue 适合快速做桌面 UI
> 4. 后续可以扩展 Agent 诊断助手
> ```
>
> ------
>
> ### 9.2 进程结构
>
> 推荐三进程：
>
> ```text
> dualnet-ui.exe
> dualnet-helper.exe
> dualnet-service.exe
> ```
>
> 职责：
>
> ```text
> dualnet-ui.exe：
> 普通权限运行，负责界面展示
> 
> dualnet-helper.exe：
> 按需管理员权限运行，负责修改网络配置
> 
> dualnet-service.exe：
> 可选后台服务，负责 DNS / DHCP / 代理 / 状态同步
> ```
>
> 不要让整个 UI 长期以管理员权限运行。
>
> ------
>
> ### 9.3 项目目录
>
> ```text
> dualnet-bridge/
> │
> ├─ apps/
> │  └─ desktop/
> │     ├─ src/
> │     ├─ src-tauri/
> │     └─ package.json
> │
> ├─ crates/
> │  ├─ net-core/
> │  ├─ net-admin/
> │  ├─ adapter-scanner/
> │  ├─ protected-adapter/
> │  ├─ nat-manager/
> │  ├─ dns-forwarder/
> │  ├─ dhcp-server/
> │  ├─ proxy-server/
> │  ├─ firewall-manager/
> │  ├─ diagnostics/
> │  └─ rollback/
> │
> ├─ configs/
> │  ├─ default.json
> │  └─ schema.json
> │
> ├─ docs/
> │  ├─ design.md
> │  ├─ network-modes.md
> │  ├─ security-boundary.md
> │  ├─ troubleshooting.md
> │  └─ recovery.md
> │
> └─ scripts/
>    ├─ dev.ps1
>    ├─ build.ps1
>    ├─ reset-gateway.ps1
>    └─ reset-client.ps1
> ```
>
> ------
>
> ## 10. UI 设计
>
> ### 10.1 Gateway 首页
>
> 显示：
>
> ```text
> 当前状态：未开启 / 已开启 / 异常
> 公司出口网卡：已保护
> 公司网卡模式：DHCP 自动获取
> 公司网卡联网状态：正常 / 异常
> 内网网卡：已选择 / 未选择
> 内网 IP：192.168.77.1
> NAT 状态：开启 / 关闭
> DNS 转发：开启 / 关闭
> Client 状态：在线 / 离线
> ```
>
> 按钮：
>
> ```text
> 扫描网卡
> 开启高级共享
> 关闭共享并恢复
> 网络诊断
> 导出日志
> ```
>
> ------
>
> ### 10.2 Gateway 网卡选择页
>
> 分为：
>
> ```text
> 公司出口网卡，只读保护
> 内网网卡，可配置
> ```
>
> 公司出口网卡显示：
>
> ```text
> 已保护，不可修改
> ```
>
> 内网网卡显示：
>
> ```text
> 可用于连接第二台电脑
> ```
>
> 如果用户选错：
>
> ```text
> 该网卡是公司网络出口，不能作为内网网卡使用。
> ```
>
> ------
>
> ### 10.3 Client 首页
>
> 显示：
>
> ```text
> Gateway：已发现 / 未发现
> Gateway IP：192.168.77.1
> 当前本机 IP：192.168.77.100
> 默认网关：192.168.77.1
> DNS：192.168.77.1
> 公网访问：正常 / 异常
> DNS 解析：正常 / 异常
> ```
>
> 按钮：
>
> ```text
> 发现 Gateway
> 自动配置网络
> 诊断连接
> 恢复原配置
> ```
>
> ------
>
> ## 11. 核心流程
>
> ### 11.1 Gateway 开启共享流程
>
> ```text
> 1. 检查管理员权限
> 2. 扫描所有网卡
> 3. 找到默认路由出口
> 4. 标记公司网卡为 ProtectedAdapter
> 5. 确认公司网卡 DHCP 自动获取
> 6. 测试公司网卡公网连通性
> 7. 选择内网网卡
> 8. 备份内网网卡配置
> 9. 设置内网网卡为 192.168.77.1/24
> 10. 检查网段冲突
> 11. 创建 NAT 规则
> 12. 启动 DNS 转发
> 13. 创建防火墙规则
> 14. 启动 Gateway 发现服务
> 15. 等待 Client 连接
> 16. 显示副电脑连接状态
> ```
>
> ------
>
> ### 11.2 Client 连接流程
>
> ```text
> 1. 检查管理员权限
> 2. 扫描本机网卡
> 3. 备份当前网络配置
> 4. 发现 Gateway
> 5. 获取 Gateway 配置
> 6. 配置本机 IP / 网关 / DNS
> 7. ping Gateway
> 8. ping 公网 IP
> 9. 测试域名解析
> 10. 回传诊断结果
> 11. 显示联网成功
> ```
>
> ------
>
> ### 11.3 关闭共享流程
>
> Gateway：
>
> ```text
> 1. 停止发现服务
> 2. 停止 DNS 转发
> 3. 停止 DHCP
> 4. 停止代理
> 5. 删除 NAT
> 6. 删除防火墙规则
> 7. 恢复内网网卡配置
> 8. 保持公司网卡不动
> ```
>
> Client：
>
> ```text
> 1. 恢复原 IP 配置
> 2. 恢复原 DNS
> 3. 恢复原网关
> 4. 恢复原代理
> 5. 清理临时状态
> ```
>
> ------
>
> ## 12. 配置文件设计
>
> Gateway 配置：
>
> ```json
> {
>   "mode": "advanced_nat",
>   "protected_adapter_policy": {
>     "enabled": true,
>     "protect_default_route_adapter": true,
>     "allow_modify_company_adapter": false
>   },
>   "internal_network": {
>     "ip": "192.168.77.1",
>     "prefix": 24,
>     "network": "192.168.77.0/24"
>   },
>   "nat": {
>     "enabled": true,
>     "name": "DualNetBridgeNat"
>   },
>   "dns": {
>     "enabled": true,
>     "listen": "192.168.77.1",
>     "upstream_mode": "company_adapter_dns_first",
>     "fallback": ["223.5.5.5", "114.114.114.114"]
>   },
>   "dhcp": {
>     "enabled": false,
>     "start": "192.168.77.100",
>     "end": "192.168.77.200"
>   },
>   "proxy": {
>     "http_enabled": false,
>     "http_port": 7890,
>     "socks5_enabled": false,
>     "socks5_port": 1080,
>     "bind_internal_only": true
>   },
>   "firewall": {
>     "auto_configure": true,
>     "internal_network_only": true
>   },
>   "rollback": {
>     "backup_before_apply": true,
>     "restore_on_failure": true
>   }
> }
> ```
>
> Client 配置：
>
> ```json
> {
>   "gateway_discovery": {
>     "udp_enabled": true,
>     "manual_gateway_ip": "192.168.77.1",
>     "port": 37777
>   },
>   "client_network": {
>     "mode": "static",
>     "ip": "192.168.77.100",
>     "prefix": 24,
>     "gateway": "192.168.77.1",
>     "dns": "192.168.77.1"
>   },
>   "rollback": {
>     "backup_before_apply": true,
>     "restore_on_failure": true
>   }
> }
> ```
>
> ------
>
> ## 13. MVP 范围
>
> 第一版高级 MVP 必须包含：
>
> ```text
> 1. Gateway 主控端
> 2. Client 客户端
> 3. 双端管理员权限检测
> 4. Gateway 网卡扫描
> 5. ProtectedAdapter 保护机制
> 6. Gateway 内网网卡配置
> 7. Gateway NAT 创建 / 删除
> 8. Client 自动发现 Gateway
> 9. Client 自动配置 IP / 网关 / DNS
> 10. 双端基础诊断
> 11. 双端一键回滚
> ```
>
> 第一版暂不做：
>
> ```text
> 1. DHCP 服务
> 2. 完整 DNS 缓存系统
> 3. HTTP / SOCKS5 代理
> 4. 流量统计
> 5. 开机自启
> 6. 多设备管理
> 7. Agent 自动修复
> 8. MAC 克隆
> 9. 公司网络绕过
> ```
>
> ------
>
> ## 14. 第二阶段功能
>
> ```text
> 1. DNS 转发增强
> 2. DHCP 服务
> 3. HTTP 代理
> 4. SOCKS5 代理
> 5. 防火墙规则可视化
> 6. 多客户端连接管理
> 7. 流量统计
> 8. 日志导出
> 9. 开机自动恢复共享
> 10. ICS 兼容模式
> ```
>
> ------
>
> ## 15. 第三阶段功能
>
> ```text
> 1. Agent 诊断助手
> 2. 自动分析失败原因
> 3. 自动生成修复方案
> 4. 多网络配置保存
> 5. Web 控制面板
> 6. 局域网文件传输
> 7. 设备命名和固定 IP
> 8. 便携版
> 9. CLI 命令行工具
> 10. 企业白名单模式
> ```
>
> ------
>
> ## 16. 安全与合规边界
>
> 本软件不提供：
>
> ```text
> 1. MAC 克隆
> 2. 绕过公司认证
> 3. 绕过 802.1X
> 4. 绕过 NAC 准入
> 5. 绕过审计
> 6. 隐藏设备
> 7. 破解网络限制
> ```
>
> 首次启动必须提示：
>
> ```text
> 本工具只用于合法网络共享与双机连接配置。
> 请确认你有权限在当前网络环境中使用网络共享。
> 软件不会修改第一台电脑公司出口网卡的 IP、网关、DNS 或认证配置。
> ```
>
> ------
>
> ## 17. 验收标准
>
> ### 17.1 Gateway 验收
>
> ```text
> 1. 公司网卡仍然是 DHCP 自动获取
> 2. 公司网卡仍然可以访问公网
> 3. 公司网卡没有被修改 IP / DNS / 网关
> 4. 内网网卡成功设置为 192.168.77.1
> 5. NAT 规则创建成功
> 6. DNS 转发启动成功
> 7. Client 可以连接 Gateway
> 8. 关闭共享后内网网卡恢复原配置
> ```
>
> ### 17.2 Client 验收
>
> ```text
> 1. Client 能发现 Gateway
> 2. Client 自动配置为 192.168.77.100
> 3. Client 默认网关为 192.168.77.1
> 4. Client DNS 为 192.168.77.1
> 5. Client 能 ping 通 192.168.77.1
> 6. Client 能 ping 通公网 IP
> 7. Client 能解析域名
> 8. Client 能打开网页
> 9. Client 可以一键恢复原配置
> ```
>
> ------
>
> ## 18. 测试命令
>
> Gateway：
>
> ```cmd
> ipconfig /all
> route print
> ping 223.5.5.5
> ping www.baidu.com
> ```
>
> Client：
>
> ```cmd
> ipconfig /all
> ping 192.168.77.1
> ping 223.5.5.5
> ping www.baidu.com
> ```
>
> 判断规则：
>
> ```text
> Client ping 192.168.77.1 不通：
> 双机内网连接问题
> 
> Client ping 192.168.77.1 通，但 ping 公网 IP 不通：
> NAT / 防火墙 / 路由问题
> 
> Client ping 公网 IP 通，但域名不通：
> DNS 问题
> 
> Gateway 自己无法上网：
> 公司网卡异常，但软件不得自动修改公司网卡
> ```
>
> ------
>
> ## 19. 开发路线
>
> ### 第 1 步：CLI 原型
>
> 先做命令行，不急着做 UI。
>
> 命令：
>
> ```text
> dualnet scan
> dualnet protect-check
> dualnet gateway-start
> dualnet gateway-stop
> dualnet client-connect
> dualnet client-restore
> dualnet diagnose
> ```
>
> 目标：
>
> ```text
> 先证明 NAT、双端配置、回滚可行。
> ```
>
> ------
>
> ### 第 2 步：Rust Core
>
> 封装核心函数：
>
> ```text
> scan_adapters()
> detect_protected_adapter()
> configure_internal_adapter()
> create_nat()
> remove_nat()
> start_dns_forwarder()
> discover_gateway()
> configure_client()
> run_diagnostics()
> rollback()
> ```
>
> ------
>
> ### 第 3 步：Tauri UI
>
> 实现：
>
> ```text
> 1. Gateway 首页
> 2. Client 首页
> 3. 网卡选择页
> 4. 诊断页
> 5. 日志页
> 6. 恢复页
> ```
>
> ------
>
> ### 第 4 步：异常恢复
>
> 提供：
>
> ```text
> reset-gateway.ps1
> reset-client.ps1
> ```
>
> 并在软件启动时自动检测残留配置。
>
> ------
>
> ### 第 5 步：体验增强
>
> 增加：
>
> ```text
> 1. DHCP
> 2. DNS 转发增强
> 3. 代理模式
> 4. 流量统计
> 5. 多客户端管理
> ```
>
> ------
>
> ## 20. 最终产品原则
>
> DualNet Bridge 的最终原则：
>
> ```text
> 1. 第一台电脑公司网卡永远只读
> 2. 默认路由所在网卡永远保护
> 3. 只修改内网侧
> 4. 所有修改前必须备份
> 5. 所有模式必须可回滚
> 6. 诊断结果必须说人话
> 7. 不提供绕过公司安全策略的功能
> 8. 高级体验必须建立在安全边界之上
> ```
>
> 最终目标：
>
> > 用户只需要在第一台电脑点击“开启共享”，在第二台电脑点击“连接主机”，软件自动完成内网配置、NAT、DNS、诊断和恢复。
>
> 这才是合理的高级体验。不是让用户手动改 IP、手动填网关、手动查 DNS，然后像网络管理员实习第一天一样在控制面板里迷路。